using UnityEngine;
using System.Collections;

public class ShipsController : MonoBehaviour
{

	private static readonly Vector3 UP_LEFT_CORNER = new Vector3();
	private static readonly Vector3 UP_RIGHT_CORNER = new Vector3();
	private static readonly Vector3 DOWN_LEFT_CORNER = new Vector3();
	private static readonly Vector3 DOWN_RIGHT_CORNER = new Vector3();

    // Use this for initialization
    void Start()
    {
       
    }

    // Update is called once per frame
    void Update()
    {

    }
}
